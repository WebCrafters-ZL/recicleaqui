// .sequelizerc

const path = require('path');

module.exports = {
    'config': path.resolve('config', 'db.js'), // Caminho para o arquivo de configuração do Sequelize
    'models-path': path.resolve('db', 'models'), // Caminho para o diretório de modelos do Sequelize
    'seeders-path': path.resolve('db', 'seeders'), // Caminho para o diretório de seeders do Sequelize
    'migrations-path': path.resolve('db', 'migrations') // Caminho para o diretório de migrations do Sequelize
};
